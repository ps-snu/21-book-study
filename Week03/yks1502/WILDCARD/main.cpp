#include <bits/stdc++.h>
using namespace std;

int N;
string wc;
string files[50];

int main() {
    int C;
    for (int i = 0; i < C; i++) {
        cin >> wc;
        cin >> N;
        for (int j = 0; j < N; j++) {
            cin >> files[j];
        }
    }
}